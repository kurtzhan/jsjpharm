<%= render "shared/content_header"%>
<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-body">
          <div class="dataTables_wrapper form-inline dt-bootstrap">
            <div class="row">
              <div class="col-sm-12">
                <table class="table table-bordered table-striped dataTable">
                  <thead>
                    <tr role="row">
                      <th>名称</th>
                      <th colspan="3"></th>
                    </tr>
                  </thead>
                  <tbody>
                  <% @member_grades.each do |member_grade| %>
                    <tr>
                      <td><%= member_grade.name %></td>
                      <td>
                        <%= link_to '<i class="fa fa-info"></i>'.html_safe, member_grade, {"data-toggle" => "tooltip", :title=>"查看"} %>
                        <%= link_to '<i class="fa fa-edit"></i>'.html_safe, edit_member_grade_path(member_grade), {"data-toggle" => "tooltip", :title=>"编辑"} %>
                        <%= link_to '<i class="fa fa-remove"></i>'.html_safe, member_grade, method: :delete, data: { confirm: 'Are you sure?' } %>
                      </td>
                      <td><%= link_to 'Edit', edit_member_grade_path(member_grade) %></td>
                      <td><%= link_to 'Destroy', member_grade, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                    </tr>
                  <% end %>
                </tbody>
                </table>
                <%= link_to '新建用户等级', new_member_grade_path, :class => "btn-primary btn-sm"%>
              </div>
            </div>

          </div>

        </div>
      </div>
    </div>
  </div>
</section>